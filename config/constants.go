package config

const (
	ATLAS_API_ROOT_URI         = "https://cloud.mongodb.com/api/atlas/v1.0"
	ATLAS_API_RELATIVE_LOG_URI = "/groups/%s/clusters/%s/logs/%s"
	ATLAS_CLUSTER_INFO_URI     = "/groups/%s/processes"
)
